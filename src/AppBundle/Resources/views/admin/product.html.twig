{% extends 'AppBundle::layout.html.twig' %}

{% block content %}

    <h1>{{ add ? 'Dodaj' : 'Edytuj' }} produkt</h1>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}